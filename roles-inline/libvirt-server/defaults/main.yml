---
# this needs a lot of work if I want to use with debian or ubuntu - only tested on RHEL 9
common_packages_libvirt_server:
  - qemu-kvm

debian_packages_libvirt_server:
  - cloud-image-utils
  - libvirt-daemon-system
  - libvirt-clients
  - virtinst
  - vagrant-libvirt
  - bridge-utils

RedHat_packages_libvirt_server:
  - libvirt
  - virt-install
  - libguestfs-tools
  - virt-top
  - cockpit
  - cockpit-machines
  - libguestfs-tools-c
