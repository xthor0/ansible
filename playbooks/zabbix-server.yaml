  - hosts: zabbix_servers
    become: true

    roles:
      - role: users
      - role: bash
      - role: geerlingguy.repo-epel
        when: ansible_os_family == "RedHat"
      - geerlingguy.mysql
      - geerlingguy.apache
      #- geerlingguy.php
      - community.zabbix.zabbix_server
      - role: community.zabbix.zabbix_web
        tags: ["zabbix-web"]
      - role: community.zabbix.zabbix_agent
        tags: ["zabbix-agent"]
      - role: zabbix-fix-httpd
        tags: ["zabbix-fix-httpd"]
      # doesn't work. don't quite know why. so there's some manual setup I have to do, unfortunately.
      #- role: zabbix-users
      #  tags: ["zabbix-users"]